[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"
  SECRETS_SCAN_OMIT_PATHS = ".env,server/.env,*.md,dist/**,server/marketplace.db"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
